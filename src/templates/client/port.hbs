/**
 * I{{service}}ClientPort
 * 
 * Port interface for {{service}} client operations.
 * Part of the application layer in Hexagonal Architecture.
 * Generated by @struktos/cli on {{timestamp}}
 */

import { RequestContext } from '{{coreImports.RequestContext}}';

/**
 * {{service}} entity representation
 */
export interface {{service}}Dto {
  id: string;
  name: string;
  description?: string;
  createdAt: Date;
  updatedAt: Date;
}

/**
 * Filter options for listing {{service}}s
 */
export interface {{service}}FilterDto {
  /** Page number (1-indexed) */
  page?: number;
  /** Items per page */
  limit?: number;
  /** Search query */
  query?: string;
  /** Sort field */
  sortBy?: string;
  /** Sort direction */
  sortOrder?: 'asc' | 'desc';
}

/**
 * Paginated list response
 */
export interface {{service}}ListDto {
  /** List of {{service}}s */
  items: {{service}}Dto[];
  /** Total count */
  total: number;
  /** Current page */
  page: number;
  /** Items per page */
  limit: number;
  /** Has more pages */
  hasMore: boolean;
}

/**
 * Input for creating a new {{service}}
 */
export interface Create{{service}}Dto {
  name: string;
  description?: string;
}

/**
 * Input for updating an existing {{service}}
 */
export interface Update{{service}}Dto {
  name?: string;
  description?: string;
}

/**
 * Port interface for {{service}} client
 * 
 * This interface defines the contract for accessing {{service}} data
 * from external services (e.g., via gRPC, REST, etc.)
 */
export interface I{{service}}ClientPort {
  /**
   * Get a single {{service}} by ID
   * 
   * @param context - Request context with trace ID
   * @param id - The {{service}} ID
   * @returns The {{service}} or throws if not found
   */
  get(context: RequestContext, id: string): Promise<{{service}}Dto>;

  /**
   * List {{service}}s with optional filtering
   * 
   * @param context - Request context with trace ID
   * @param filter - Optional filter and pagination options
   * @returns Paginated list of {{service}}s
   */
  list(context: RequestContext, filter?: {{service}}FilterDto): Promise<{{service}}ListDto>;

  /**
   * Create a new {{service}}
   * 
   * @param context - Request context with trace ID
   * @param data - The {{service}} data to create
   * @returns The created {{service}}
   */
  create(context: RequestContext, data: Create{{service}}Dto): Promise<{{service}}Dto>;

  /**
   * Update an existing {{service}}
   * 
   * @param context - Request context with trace ID
   * @param id - The {{service}} ID to update
   * @param data - The fields to update
   * @returns The updated {{service}}
   */
  update(context: RequestContext, id: string, data: Update{{service}}Dto): Promise<{{service}}Dto>;

  /**
   * Delete a {{service}}
   * 
   * @param context - Request context with trace ID
   * @param id - The {{service}} ID to delete
   * @returns True if deleted successfully
   */
  delete(context: RequestContext, id: string): Promise<boolean>;
}